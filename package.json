{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r build && node packages/cli/dist-cli/index.js .",
    "lint": "biome check .",
    "format": "biome format . --write",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "typescript": "~5.9.3",
    "vitest": "^4.1.2"
  }
}
